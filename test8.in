int main() {
  tata(2094,23,55);
  mama(2094,23,55,7);
  if (5 < 0) then { 44 }
  else { div(2094, 23, 55) }
}

int div(int n, int q, int tmp) {
  tmp := 0;
  repeat { n := (n - q); tmp := (tmp + 1) } until (n < 0);
  tmp
}

bool tata(int n, int q, int tmp) {
  tmp := 0;
  repeat { n := (n - q); tmp := (tmp + 1) } until (n < 0);
  tmp := (4+2);
  (3>2)
}

unit mama(int n, int q, int tmp, int o) {
  tmp := 0;
  repeat { n := (n - q); tmp := (tmp + 1) } until (n < 0);
  while (o > 5) do {
    o := (o -1)
  }
}